databaseChangeLog:
  - changeSet:
      id: create-role-table
      author: daniil
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: role
                  type: ENUM('ROLE_USER', 'ROLE_ADMIN')
                  constraints:
                    nullable: false
                    unique: true
